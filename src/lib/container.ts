/**
 * Unified container classes for consistent layout across the app.
 * Use these for all pages and components to ensure uniform width and padding.
 */

/** Main container: max-w-7xl, centered, with px-4 md:px-8 */
export const containerClass = "max-w-7xl mx-auto px-4 md:px-8";

/** Content wrapper for sections (e.g. hero, features, footer) */
export const containerSectionClass = "max-w-7xl mx-auto px-4 md:px-8";

/** Narrower content for long-form text (eula, privacy) - centered within container */
export const containerProseClass = "max-w-3xl mx-auto";

/** Form/quiz content width */
export const containerFormClass = "max-w-lg mx-auto";
